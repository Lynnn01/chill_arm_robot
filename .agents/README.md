# Agent Workspace Configuration

This directory contains workspace-specific rules and instructions for AI agents.

## Structure
- `AGENTS.md`: The main entry point containing critical rules and an index of detailed rules.
- `rule/`: Contains detailed, category-specific markdown rules (e.g., `style.md`, `auth.md`).
- `agents/`: Custom subagent definitions.
- `skills/`: Custom local skills.
